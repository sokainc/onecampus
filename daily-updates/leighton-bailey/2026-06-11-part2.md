# Daily Update - 2026-06-11 (Part 2)

## What I worked on today
- Published the native app to Expo's servers (EAS Update) so it loads in Expo Go from anywhere — no computer needed
- Added a QR code "Get the App" section to the website that opens One Campus in Expo Go
- Set up REAL authentication with Firebase: Google sign-in on the website, email/password accounts in the app
- Built the cloud backend with Firestore — user points, clubs, RSVPs, and settings now save per account
- Added settings customization: 5 accent color themes, editable profile, notification controls, quiet hours, reset data
- Added in-app walking directions with turn-by-turn steps on a real campus map (no switching to Google Maps)
- Created the project README
- Removed the team section from the website

## What I completed
- EAS publish: app live at expo.dev/accounts/leightonb23/projects/onecampus-app on the production channel (SDK 54)
- Firebase Authentication: Google provider + Email/Password provider both enabled and tested
- Firestore database created with production-grade security rules (each user can only read/write their own data)
- VERIFIED END TO END with live tests: create account ✓ sign in ✓ save data to cloud ✓ read it back ✓ sign out ✓
- Native map in the app (Apple/Google Maps via react-native-maps) with building, friend, and route pins
- Turn-by-turn walking directions from OpenStreetMap routing — real sidewalk paths with distances per step
- Google account picker fix so users can switch accounts at sign-in

## Problems or bugs
- QR code gave a 404 — the Expo "production" channel didn't exist yet; fixed with eas channel:create
- Google sign-in gave auth/configuration-not-found — the Google provider wasn't enabled in Firebase yet
- Firestore writes failed 404 then 403 — database wasn't created, then rules were locked; fixed by creating the DB and publishing custom security rules
- PowerShell blocked npx (execution policy) — used npx.cmd instead
- Phone internet was flaky so on-phone testing is still pending

## What I need help with
- Testing the full flow on my phone once internet cooperates (scan QR → create account → data syncs)
- Enabling GitHub Pages for a public website link
- Eventually: custom domain so the Google sign-in screen shows our own domain name

## Plan for tomorrow
- Onboarding flow: pick campus, major, and interests on first launch
- Real GPS location for the campus map (expo-location)
- Show 5 classmates the app and collect feedback
- Start thinking about the App Store path (Apple Developer account + Sign in with Apple)
